<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mumbai Visuals | Portfolio</title>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@500;700&family=Roboto:wght@300;400&display=swap" rel="stylesheet">
    <style>
        /* --- RESET & BASE --- */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        html { scroll-behavior: smooth; }

        body {
            background-color: #000;
            color: #fff;
            font-family: 'Roboto', sans-serif;
            overflow-x: hidden;
        }

        /* --- NAVIGATION --- */
        nav {
            position: fixed; /* Fixed so it stays while scrolling */
            top: 0;
            width: 100%;
            padding: 25px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 100;
            background: linear-gradient(to bottom, rgba(0,0,0,0.7), transparent);
        }
        .logo {
            font-family: 'Oswald', sans-serif;
            font-size: 1.2rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            border: 2px solid #fff;
            padding: 5px 15px;
            cursor: pointer;
        }

        /* --- HERO SECTION --- */
        .hero {
            height: 100vh;
            width: 100%;
            background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.8)), 
                              url('https://images.unsplash.com/photo-1570160897040-30430ade2211?auto=format&fit=crop&w=1920&q=80');
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        h1 {
            font-family: 'Oswald', sans-serif;
            /* Fluid Typography: Min 3rem, Max 12rem */
            font-size: clamp(3rem, 12vw, 12rem);
            line-height: 0.9;
            text-transform: uppercase;
            letter-spacing: -2px;
            margin-bottom: 20px;
        }

        .subtitle {
            font-size: clamp(1rem, 2vw, 1.5rem);
            letter-spacing: 5px;
            text-transform: uppercase;
            background-color: #fff;
            color: #000;
            padding: 10px 25px;
            font-weight: bold;
        }

        /* --- GALLERY GRID --- */
        .gallery-section {
            padding: 100px 5%;
            background-color: #000;
        }

        .section-header {
            font-family: 'Oswald', sans-serif;
            font-size: clamp(2rem, 5vw, 4rem);
            margin-bottom: 50px;
            border-bottom: 1px solid #333;
            padding-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 30px;
        }

        /* --- CARD & HOVER EFFECTS --- */
        .card {
            position: relative;
            overflow: hidden;
            aspect-ratio: 4 / 5; /* Modern way to handle height */
            cursor: pointer;
            background-color: #111;
        }

        .card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: grayscale(100%);
            transition: transform 0.8s cubic-bezier(0.2, 1, 0.3, 1), filter 0.5s ease;
        }

        .card:hover img {
            transform: scale(1.08);
            filter: grayscale(0%);
        }

        .card-overlay {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.5);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            opacity: 0;
            transition: opacity 0.4s ease;
        }

        .card-overlay span {
            font-family: 'Oswald', sans-serif;
            font-size: 1.5rem;
            letter-spacing: 2px;
            transform: translateY(20px);
            transition: transform 0.4s ease;
        }

        .card:hover .card-overlay { opacity: 1; }
        .card:hover .card-overlay span { transform: translateY(0); }

        /* --- LIGHTBOX MODAL --- */
        #lightbox {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.95);
            display: none; /* Hidden by default */
            justify-content: center;
            align-items: center;
            z-index: 1000;
            cursor: zoom-out;
        }

        #lightbox img {
            max-width: 90%;
            max-height: 80%;
            box-shadow: 0 0 50px rgba(0,0,0,0.5);
            border: 5px solid #fff;
        }

        /* --- FOOTER --- */
        footer {
            padding: 100px 5%;
            text-align: center;
            background-color: #000;
        }

        .contact-btn {
            font-family: 'Oswald', sans-serif;
            font-size: clamp(1.5rem, 4vw, 3rem);
            text-decoration: none;
            color: #fff;
            border: 2px solid #fff;
            padding: 20px 60px;
            transition: all 0.4s ease;
            display: inline-block;
        }

        .contact-btn:hover {
            background-color: #fff;
            color: #000;
            transform: translateY(-5px);
        }

        /* --- RESPONSIVE TWEAKS --- */
        @media (max-width: 768px) {
            .grid { grid-template-columns: 1fr; }
            .hero { height: 80vh; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">MUMBAI VISUALS</div>
    </nav>

    <header class="hero">
        <h1>BEYOND<br>THE CHAOS</h1>
        <div class="subtitle">Visual Storyteller</div>
    </header>

    <section class="gallery-section">
        <div class="section-header">
            <span>PORTFOLIO</span>
            <small style="font-size: 1rem; color: #666;">2024 — 2026</small>
        </div>
        
        <div class="grid">
            <div class="card" onclick="openLightbox(this)">
                <img src="https://images.unsplash.com/photo-1566552881560-0be862a7c445?auto=format&fit=crop&w=800&q=80" alt="Marine Drive">
                <div class="card-overlay"><span>MARINE DRIVE</span></div>
            </div>
            <div class="card" onclick="openLightbox(this)">
                <img src="https://images.unsplash.com/photo-1595658658481-d53d3f999875?auto=format&fit=crop&w=800&q=80" alt="Local Train">
                <div class="card-overlay"><span>THE LIFELINE</span></div>
            </div>
            <div class="card" onclick="openLightbox(this)">
                <img src="https://images.unsplash.com/photo-1621610444589-3221975e4785?auto=format&fit=crop&w=800&q=80" alt="Dharavi">
                <div class="card-overlay"><span>STREET CULTURE</span></div>
            </div>
            <div class="card" onclick="openLightbox(this)">
                <img src="https://images.unsplash.com/photo-1567157577867-05ccb1388e66?auto=format&fit=crop&w=800&q=80" alt="Gateway of India">
                <div class="card-overlay"><span>COLONIAL ECHOES</span></div>
            </div>
        </div>
    </section>

    <div id="lightbox" onclick="closeLightbox()">
        <img id="lightbox-img" src="" alt="Zoomed view">
    </div>

    <footer>
        <p style="margin-bottom: 40px; color: #555; letter-spacing: 2px;">LET'S TELL YOUR STORY</p>
        <a href="mailto:hello@mumbaivisuals.com" class="contact-btn">GET IN TOUCH</a>
    </footer>

    <script>
        // Lightbox Logic
        const lightbox = document.getElementById('lightbox');
        const lightboxImg = document.getElementById('lightbox-img');

        function openLightbox(card) {
            const imgSrc = card.querySelector('img').src;
            // Use the high-res version of the image for the lightbox
            lightboxImg.src = imgSrc.replace('w=800', 'w=1600');
            lightbox.style.display = 'flex';
            document.body.style.overflow = 'hidden'; // Stop scrolling
        }

        function closeLightbox() {
            lightbox.style.display = 'none';
            document.body.style.overflow = 'auto'; // Resume scrolling
        }
    </script>

</body>
</html>